[build-system]
requires = ["uv_build>=0.9.9,<0.10.0"]
build-backend = "uv_build"

[project]
name = "mypackage"
version = "0.1.0"
description = ""
authors = []
requires-python = ">=3.10"
dependencies = [
    "httpx",
]
