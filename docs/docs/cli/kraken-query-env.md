# kraken query env

!!! warning "Deprecated"

    This command is deprecated since `kraken-build v0.45.0` and will be removed in a future version. It was used
    by `kraken-wrapper` to generate the lockfile. Since `v0.45.0`, it queries Uv to generate the lockfile.

<!-- runcmd code: kraken query env --help | sed -r "s/\x1B\[([0-9]{1,3}(;[0-9]{1,2})?)?[mGK]//g" -->
<!-- end runcmd -->
