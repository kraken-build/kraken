[project]
name = "rust-pdm-project"
version = "0.1.0"
description = ""
authors = []
requires-python = ">=3.9"

[tool.pdm.dev-dependencies]
build = ["maturin~=1.0", "pip~=23.0"]
lint = ["mypy~=1.0"]

[build-system]
requires = ["maturin~=1.0"]
build-backend = "maturin"
