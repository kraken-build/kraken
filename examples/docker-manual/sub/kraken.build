
from kraken.std.util.render_file_task import RenderFileTask

project.task "helloWorld" RenderFileTask {
    default = True
    file.set(project.build_directory / "out.txt")
    content.set("Hello, World!")
}
